# ==========================================
# Excel → JSON Automation - Python Dependencies
# ==========================================
# NOTE:
# This file lists all the Python packages required to run the
# 'create_update_backup_delete.py' script. The GitHub Actions
# workflow uses this file to install the correct dependencies.
# ==========================================

# === Core Data Handling ===
pandas>=2.0.0            # For reading and processing data from Excel files
openpyxl>=3.1.0          # The engine pandas uses to read modern .xlsx Excel files

# === Web Scraping & Networking ===
requests>=2.31.0         # For making HTTP requests to download web pages and images
beautifulsoup4>=4.12.0   # The primary library for parsing HTML content from websites
lxml>=4.9.0              # A high-performance parser that BeautifulSoup4 can use
duckduckgo-search>=4.0.0 # For using DuckDuckGo as a search engine to find show pages

# === Image Processing ===
Pillow>=10.0.0           # For resizing and saving downloaded show images in a standard format

# === Google Drive API Integration ===
# These packages are required to securely connect to the Google Drive API
# and download the private Excel file.
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.0
google-auth>=2.23.0