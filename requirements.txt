# ============================================================================
# requirements.txt
# Purpose: Python dependencies for the Excel → JSON automation script.
#
# Keep this file minimal: only packages the script actually uses.
# When upgrading packages, prefer pinning major versions to avoid surprises.
# ============================================================================
pandas==2.2.2
openpyxl>=3.1.2
requests>=2.31.0
Pillow>=10.2.0
ddgs==9.5.5
beautifulsoup4>=4.12.2
lxml>=4.9.3
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-httplib2>=0.1.0
google-auth-oauthlib>=1.1.0
